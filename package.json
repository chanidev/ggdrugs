{
  "name": "ggdrugs",
  "private": true,
  "version": "0.0.0",
  "description": "Alle — 자연어 처리 기반 이벤트·이슈 지도 검색 서비스 (서울)",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r --filter=./packages/** --filter=./apps/** build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "pnpm": {
    "overrides": {
      "rollup": "npm:@rollup/wasm-node@4.60.4"
    }
  }
}
