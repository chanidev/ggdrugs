# Slice 7: i18n 6개국어 + 게시글 번역 — Final Implementation Plan

> **리뷰 이슈 23개 전량 반영 최종본.**  
> For agentic workers: use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans`.  
> Steps use `- [ ]` checkbox syntax for tracking.

**Goal:** react-i18next 기반 6개국어(ko/en/vi/zh/ja/fr) 전체 서비스 i18n + 게시글 본문 온디맨드 번역(BFF+LLM+Redis 캐시) + GG-COMM-013 언어 토글 실연결.

**Architecture:** public/locales/{lang}/{namespace}.json 정적 파일을 i18next-http-backend로 런타임 로드. react-i18next Provider를 main.tsx AuthProvider 바깥에 감싸고 localStorage `i18n_language` 키로 언어를 영속. Suspense 부재 문제는 `react: { useSuspense: false }` 설정으로 해결. 게시글 번역은 BFF `POST /community/posts/:id/translate` → services/llm `/translate-post` → Redis TTL 7d 캐시 패턴으로 온디맨드 처리.

**컷 금지 원칙:** MVP 컷 / ko+en만 / 일부 화면만 / placeholder 키 일절 없음. 미번역 키 0을 deep-diff 검증으로 실증.

**Tech Stack:** react-i18next 15.x, i18next 24.x, i18next-browser-languagedetector 8.x, i18next-http-backend (web); FastAPI + OpenAI gpt-4o-mini (llm); ioredis TTL (bff)

---

## 전체 파일 지도

### 신규 생성 (web)
- `apps/web/src/lib/i18n.ts` — i18next 초기화 (LanguageDetector + localStorage + useSuspense:false)
- `apps/web/src/lib/useLanguage.ts` — 언어 전환 훅
- `apps/web/src/components/LanguageToggle.tsx` — 드롭다운 언어 선택기
- `apps/web/src/lib/api/translate.ts` — 게시글 번역 fetch 함수
- `apps/web/public/locales/ko/*.json` — 한국어 원본 (8 네임스페이스)
- `apps/web/public/locales/en/*.json` ~ `fr/*.json` — 5개 언어 번역 번들 (5×8=40개)

### 신규 생성 (bff)
- `apps/bff/src/routes/translate.ts` — POST /community/posts/:id/translate 핸들러
- `apps/bff/src/lib/translation-cache.ts` — Redis TTL 캐시 helpers (키 prefix: `post:translation:{postId}:{lang}`)

### 신규 생성 (llm)
- `services/llm/translate.py` — `/translate-post`, `/translate-bundle` FastAPI 엔드포인트

### 수정 (web)
- `apps/web/package.json` — react-i18next 4개 패키지 추가
- `apps/web/src/main.tsx` — I18nextProvider 삽입 (Suspense 없이 동작)
- `apps/web/src/styles/index.css` — 다국어 폰트 fallback (`--font-sans` 체인 확장: Noto Sans JP / Noto Sans SC / Noto Sans)
- `DESIGN.md` — 다국어 폰트 예외 절 추가 (코드 전 선행)
- `apps/web/src/layout/Header.tsx`
- `apps/web/src/layout/Sidebar.tsx`
- `apps/web/src/layout/AppShell.tsx`
- `apps/web/src/layout/MobileShell.tsx` — 모바일 탭 라벨 (목록/필터/채팅) + 안내 문자열 (이슈 2/10)
- `apps/web/src/components/mobile/BottomSheet.tsx` — aria-label 한국어 문자열 (이슈 2/10)
- `apps/web/src/pages/CommunityPage/index.tsx` + `parts/CommunityShell.tsx` + `parts/ComposeModal.tsx` + `parts/PostList.tsx` + `parts/CategoryGrid.tsx`
- `apps/web/src/pages/PostDetailPage/index.tsx` + `parts/CommentComposer.tsx` + `parts/CommentTree.tsx` + `parts/AuthorProfileModal.tsx`
- `apps/web/src/pages/MyPage/index.tsx` + `tabs/BookmarksTab.tsx` + `tabs/ReviewsTab.tsx` + `tabs/RecommendationsTab.tsx` + `tabs/SubscriptionsTab.tsx` + `tabs/CalendarTab.tsx`
- `apps/web/src/pages/EventDetailPage/index.tsx` + `parts/*.tsx`
- `apps/web/src/pages/AdminEventsPage/index.tsx` + `tabs/EventsTab.tsx` + `tabs/UploadersTab.tsx` + `tabs/UploadReviewsTab.tsx` + `tabs/ReportsTab.tsx`
- `apps/web/src/components/admin/MembersTab.tsx` — (이슈 1/9)
- `apps/web/src/components/admin/AuditLogsTab.tsx` — (이슈 1/9)
- `apps/web/src/components/admin/UploadReviewPanel.tsx` — (이슈 1/9)
- `apps/web/src/components/admin/UserDetailPanel.tsx` — reason textarea, scope 라벨, 액션 라벨, 상태 라벨 (이슈 1/9)
- `apps/web/src/components/admin/UploaderDetailPanel.tsx` — (이슈 1/9)
- `apps/web/src/components/admin/audit/AuditDashboard.tsx` — (이슈 1/9)
- `apps/web/src/pages/UploaderPage.tsx` + `UploaderNewEventPage.tsx` + `UploaderEventEditPage.tsx`
- `apps/web/src/components/uploader/EventFormFields.tsx` — 카테고리/동행 enum 라벨 (이슈 3/12)
- `apps/web/src/components/uploader/DocumentsPickerField.tsx` — (이슈 12)
- `apps/web/src/components/uploader/PosterPickerField.tsx` — (이슈 12)
- `apps/web/src/pages/MateFormPage/index.tsx` + `parts/*.tsx`
- `apps/web/src/pages/MateRecommendationsPage/index.tsx`
- `apps/web/src/pages/ChatRequestPage/index.tsx`
- `apps/web/src/pages/ChatRoomPage/index.tsx`
- `apps/web/src/pages/EvaluationPage/index.tsx` + `parts/FestivalStep.tsx` + `parts/MateEvalStep.tsx`
- `apps/web/src/pages/NotificationsPage.tsx`
- `apps/web/src/pages/CreditPage/index.tsx`
- `apps/web/src/pages/ProfilePage/index.tsx`
- `apps/web/src/components/FilterSearchPanel.tsx` — 필터 5종 옵션 라벨 + 구독 토스트 (이슈 3/11)
- `apps/web/src/components/EventList.tsx` — (이슈 3/11)
- `apps/web/src/components/SeoulMap.tsx` — (이슈 3/11)
- `apps/web/src/components/EventSummaryPanel.tsx` — (이슈 3/11)
- `apps/web/src/components/FullListPanel.tsx`
- `apps/web/src/components/ChatDock.tsx`
- `apps/web/src/components/ChatHelpPanel.tsx`
- `apps/web/src/components/ReportModal.tsx`

### 수정 (bff)
- `apps/bff/src/app.ts` — translate 라우트 등록 (redis-client.ts 수정 없음 — import 재사용, 이슈 6)

### 수정 (llm)
- `services/llm/app.py` — /translate-post, /translate-bundle 엔드포인트 append (ChatSession/chat-eval 코드 수정 금지)

### 신규 생성 (bff jobs)
- `apps/bff/src/jobs/generate-i18n-bundles.ts` — ko→5언어 번들 생성 스크립트

---

## 네임스페이스 설계 (8개)

| 파일명 | 대상 화면군 |
|---|---|
| `common.json` | Header, 공용 버튼/상태, ErrorBoundary, MobileShell 공통 |
| `navigation.json` | Sidebar, AppShell 탐색, FilterSearchPanel 필터 5종 옵션 라벨, FullListPanel |
| `community.json` | CommunityPage 전체, PostDetailPage, 댓글, ComposeModal |
| `mate.json` | MateFormPage, MateRecommendationsPage, AuthorProfileModal |
| `chat.json` | ChatRoomPage, ChatRequestPage, ChatDock, ChatHelpPanel |
| `uploader.json` | UploaderPage, UploaderNewEventPage, UploaderEventEditPage, EventFormFields, DocumentsPickerField, PosterPickerField |
| `admin.json` | AdminEventsPage 전체 탭 + components/admin/* 패널 전체 (detail-level 포함) |
| `mypage.json` | MyPage 탭 전체, EvaluationPage, CreditPage, ProfilePage, NotificationsPage |

---

## Task 0: DESIGN.md 다국어 폰트 예외 절 추가

> **규칙: DESIGN.md에 없는 결정은 코드 전에 문서 개정 선행 (이슈 4/21)**

**Files:** `DESIGN.md`

- [ ] **Step 0-1: DESIGN.md Typography 섹션 끝에 다국어 폰트 예외 절 추가**

DESIGN.md의 Typography 섹션 (현재 "로딩 전략" 뒤) 에 다음 절을 추가한다:

```markdown
### 다국어 폰트 Fallback (i18n 예외)

기본 전략은 Pretendard 단일 패밀리이나, 6개국어 서비스(Slice 7~)에서 zh/ja/vi 렌더 시
Han unification 글리프 왜곡·tofu 방지를 위해 언어별 fallback을 추가한다.

구현: `index.css`의 `--font-sans` 커스텀 프로퍼티 체인에서 **기존 OS/한국어 fallback을 유지한 채** `sans-serif` 직전에 삽입(extend, 교체 아님).

실제 index.css 전체 체인 (DESIGN.md §다국어 폰트 Fallback과 동일):

```css
--font-sans: 'Pretendard Variable', Pretendard, -apple-system, BlinkMacSystemFont,
  system-ui, Roboto, 'Helvetica Neue', 'Segoe UI', 'Apple SD Gothic Neo',
  'Noto Sans KR', 'Malgun Gothic', 'Apple Color Emoji', 'Segoe UI Emoji',
  'Noto Sans JP', 'Noto Sans SC', 'Noto Sans', sans-serif;
```

로딩: Google Fonts CDN에서 `display=swap`으로 비동기 로드. Pretendard 이후 fallback이므로
한국어/영어/프랑스어는 영향 없음.

금지: Pretendard 자체를 제거하거나 언어별 완전히 다른 패밀리로 교체하는 것. 기존 OS/한국어 fallback 체인(-apple-system, Noto Sans KR 등)을 제거하는 것.
```

- [ ] **Step 0-2: 커밋**

```bash
git add DESIGN.md
git commit -m "docs(design): 다국어 폰트 fallback 예외 절 추가 (Slice 7 선행)"
```

---

## Task 1: react-i18next 설치 및 i18n 인프라

**Files:**
- `apps/web/package.json`
- `apps/web/src/lib/i18n.ts` (신규)
- `apps/web/src/lib/useLanguage.ts` (신규)
- `apps/web/src/main.tsx`
- `apps/web/src/styles/index.css`
- `apps/web/public/locales/` 스켈레톤

- [ ] **Step 1-1: 패키지 설치**

```bash
cd apps/web && pnpm add react-i18next i18next i18next-browser-languagedetector i18next-http-backend
```

Expected: `apps/web/package.json` dependencies에 4개 패키지 추가됨. (이슈 16)

- [ ] **Step 1-2: i18n.ts 생성**

Create `apps/web/src/lib/i18n.ts`:

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';
import HttpBackend from 'i18next-http-backend';

export type SupportedLanguage = 'ko' | 'en' | 'vi' | 'zh' | 'ja' | 'fr';

export const SUPPORTED_LANGUAGES: Array<{
  code: SupportedLanguage;
  label: string;
  nativeLabel: string;
}> = [
  { code: 'ko', label: 'Korean',     nativeLabel: '한국어' },
  { code: 'en', label: 'English',    nativeLabel: 'English' },
  { code: 'vi', label: 'Vietnamese', nativeLabel: 'Tiếng Việt' },
  { code: 'zh', label: 'Chinese',    nativeLabel: '中文' },
  { code: 'ja', label: 'Japanese',   nativeLabel: '日本語' },
  { code: 'fr', label: 'French',     nativeLabel: 'Français' },
];

export const NAMESPACES = [
  'common', 'navigation', 'community', 'mate', 'chat',
  'uploader', 'admin', 'mypage',
] as const;

void i18n
  .use(HttpBackend)
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    fallbackLng: 'ko',
    supportedLngs: ['ko', 'en', 'vi', 'zh', 'ja', 'fr'],
    defaultNS: 'common',
    ns: NAMESPACES,
    backend: {
      loadPath: '/locales/{{lng}}/{{ns}}.json',
    },
    detection: {
      order: ['localStorage', 'navigator'],
      lookupLocalStorage: 'i18n_language',
      caches: ['localStorage'],
    },
    interpolation: {
      escapeValue: false,
    },
    // [이슈 5] useSuspense:false — Suspense 경계 없이 빈 화면/에러 방지.
    // 번역 로드 전 키 원문(ko) 표시 후 교체되므로 UX 영향 최소.
    react: {
      useSuspense: false,
    },
  });

export default i18n;
```

- [ ] **Step 1-3: useLanguage.ts 생성**

Create `apps/web/src/lib/useLanguage.ts`:

```typescript
import { useTranslation } from 'react-i18next';
import type { SupportedLanguage } from './i18n.js';
import { SUPPORTED_LANGUAGES } from './i18n.js';

export function useLanguage() {
  const { i18n } = useTranslation();
  const current = (i18n.resolvedLanguage ?? i18n.language ?? 'ko') as SupportedLanguage;

  const setLanguage = async (lang: SupportedLanguage): Promise<void> => {
    await i18n.changeLanguage(lang);
    localStorage.setItem('i18n_language', lang);
  };

  return { current, setLanguage, languages: SUPPORTED_LANGUAGES };
}
```

- [ ] **Step 1-4: main.tsx에 I18nextProvider 삽입** (이슈 5/17)

`apps/web/src/main.tsx`에 import 추가:
```tsx
import { I18nextProvider } from 'react-i18next';
import i18n from './lib/i18n.js';
```

render를 다음으로 교체 (AuthProvider 바깥 래핑, Suspense 불필요 — useSuspense:false):
```tsx
createRoot(rootEl).render(
  <I18nextProvider i18n={i18n}>
    <AuthProvider>
      <BrowserRouter>
        {/* ...기존 Routes 그대로... */}
      </BrowserRouter>
    </AuthProvider>
  </I18nextProvider>,
);
```

- [ ] **Step 1-5: index.css 다국어 폰트 fallback 추가** (이슈 4/13/21)

`apps/web/src/styles/index.css`에서 `--font-sans` 정의를 찾아 **기존 체인을 유지하면서** `sans-serif` 직전에 `'Noto Sans JP'`, `'Noto Sans SC'`, `'Noto Sans'`를 삽입한다. 기존 OS/한국어 fallback(-apple-system, BlinkMacSystemFont, Noto Sans KR 등)을 제거하지 않는다.

파일 최상단(`@import 'tailwindcss'` 이전)에 Google Fonts @import를 추가한다:

```css
/* 다국어 폰트 fallback: zh/ja tofu 방지 (DESIGN.md 다국어 폰트 예외 절) */
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&family=Noto+Sans+SC:wght@400;500;700&family=Noto+Sans:wght@400;500;700&display=swap');
```

그리고 `@theme` 블록 내 `--font-sans` 값을 다음으로 교체한다 (기존 OS fallback 체인 유지, sans-serif 직전에 Noto 패밀리 삽입):

```css
--font-sans: 'Pretendard Variable', Pretendard, -apple-system, BlinkMacSystemFont,
  system-ui, Roboto, 'Helvetica Neue', 'Segoe UI', 'Apple SD Gothic Neo',
  'Noto Sans KR', 'Malgun Gothic', 'Apple Color Emoji', 'Segoe UI Emoji',
  'Noto Sans JP', 'Noto Sans SC', 'Noto Sans', sans-serif;
```

> 주의: DESIGN.md 코드 예시와 위 체인은 동일하다(14개 항목). 추가/변경 시 양쪽을 동시에 수정한다.

- [ ] **Step 1-6: locales 스켈레톤 생성**

`apps/web/public/locales/` 하위 6개 언어 × 8 네임스페이스 = 48개 파일을 빈 객체 `{}`로 생성. Task 2~3에서 실내용 채움.

```
apps/web/public/locales/
├── ko/{common,navigation,community,mate,chat,uploader,admin,mypage}.json
├── en/ (동일 8개)
├── vi/ (동일 8개)
├── zh/ (동일 8개)
├── ja/ (동일 8개)
└── fr/ (동일 8개)
```

- [ ] **Step 1-7: typecheck**

```bash
cd apps/web && pnpm typecheck
```

Expected: 오류 없음.

- [ ] **Step 1-8: 커밋**

```bash
git add apps/web/package.json apps/web/src/lib/i18n.ts apps/web/src/lib/useLanguage.ts apps/web/src/main.tsx apps/web/src/styles/index.css apps/web/public/locales/
git commit -m "feat(web): react-i18next 인프라 — provider, LanguageDetector, useSuspense:false, 다국어 폰트 fallback, 6언어 스켈레톤"
```

---

## Task 2-A: ko 원본 — common / navigation / community + 해당 화면 키화

**화면군:** Header, Sidebar, MobileShell(탭 라벨), BottomSheet(aria), CommunityShell, CommunityPage, PostDetailPage, CommentTree, CommentComposer, AuthorProfileModal, ComposeModal, PostList, CategoryGrid

**Files:**
- Create: `apps/web/public/locales/ko/common.json`
- Create: `apps/web/public/locales/ko/navigation.json`
- Create: `apps/web/public/locales/ko/community.json`
- Modify: Header.tsx, Sidebar.tsx, AppShell.tsx, MobileShell.tsx, BottomSheet.tsx, CommunityShell.tsx, CommunityPage/index.tsx, PostDetailPage/index.tsx, parts/CommentTree.tsx, parts/CommentComposer.tsx, parts/AuthorProfileModal.tsx, parts/ComposeModal.tsx, parts/PostList.tsx, parts/CategoryGrid.tsx

- [ ] **Step 2A-1: ko/common.json 생성**

```json
{
  "button": {
    "login": "로그인",
    "logout": "로그아웃",
    "write": "글쓰기",
    "edit": "수정",
    "delete": "삭제",
    "cancel": "취소",
    "confirm": "확인",
    "close": "닫기",
    "submit": "제출",
    "save": "저장",
    "report": "신고",
    "translate": "번역하기",
    "translateAnother": "다른 언어로 번역",
    "copy": "복사",
    "retry": "다시 시도",
    "back": "뒤로",
    "backToList": "목록으로"
  },
  "label": {
    "loading": "불러오는 중…",
    "error": "오류가 발생했어요.",
    "notFound": "존재하지 않거나 만료된 항목이에요.",
    "empty": "항목이 없어요.",
    "credits": "크레딧",
    "notifications": "알림",
    "chatRoom": "채팅방",
    "admin": "관리자",
    "uploader": "업로더",
    "myPage": "마이페이지",
    "login_kakao": "Kakao",
    "login_google": "Google 로그인",
    "login_google_short": "Google"
  },
  "aria": {
    "adminConsole": "관리자 콘솔",
    "uploaderConsole": "업로더 콘솔",
    "myPage": "마이페이지",
    "quickSearch": "빠른 검색 (⌘K) — 준비 중입니다",
    "quickSearchLabel": "빠른 검색 (⌘K) — 준비 중",
    "eventSheet": "이벤트 시트",
    "sheetCollapse": "시트 절반으로 축소",
    "sheetExpandFull": "시트 전체 펼치기",
    "sheetExpand": "시트 펼치기"
  },
  "search": {
    "placeholder": "이벤트·장소 검색"
  },
  "error": {
    "loginRequired": "로그인이 필요해요.",
    "forbidden": "권한이 없어요.",
    "loadFailed": "불러오지 못했어요.",
    "deleteFailed": "삭제하지 못했어요.",
    "networkError": "응답을 받지 못했어요. 잠시 후 다시 시도해 주세요.",
    "llmUnreachable": "LLM 서비스에 연결하지 못했어요."
  }
}
```

- [ ] **Step 2A-2: ko/navigation.json 생성** (이슈 3/11 — 필터 5종 옵션 라벨 포함)

```json
{
  "sidebar": {
    "eyebrow": "이벤트를 찾는\n서울의 방법",
    "description": "지도 위 핀과 채팅, 필터로 축제·박람회·심포지움·컨퍼런스를 탐색하세요.",
    "filter": { "title": "필터 검색", "description": "5개 축으로 좁히기" },
    "list":   { "title": "전체목록 조회", "description": "카테고리별 인덱스" },
    "chat":   { "title": "채팅방 검색", "description": "자연어로 묻기" }
  },
  "stats": {
    "label": "현재 공개 이벤트",
    "total": "전체",
    "ongoing": "진행중",
    "upcoming": "예정"
  },
  "mobile": {
    "tabs": {
      "list":   "목록",
      "filter": "필터",
      "chat":   "채팅"
    },
    "backToList": "목록으로",
    "eventSummary": "이벤트 요약",
    "chatHint": "자연어로 질문하면 5개 필터 + AI 의미 검색 후보를 함께 드려요.",
    "chatPlaceholder": "\"이번 주말 가족이랑 볼만한 축제\"",
    "chatAriaInput": "자연어로 이벤트 검색",
    "chatAriaSend": "검색 전송",
    "aiCandidates": "AI 후보 {{count}}건 · 의미 기반"
  },
  "filter": {
    "region": {
      "label": "지역",
      "seoul": "서울",
      "all": "전체"
    },
    "period": {
      "label": "기간",
      "3m": "3개월",
      "6m": "6개월",
      "all": "전체",
      "custom": "직접 설정"
    },
    "companion": {
      "label": "인원구성",
      "solo": "혼자",
      "couple": "커플",
      "family": "가족",
      "friend": "친구"
    },
    "eventType": {
      "label": "이벤트 종류",
      "festival": "축제",
      "expo": "박람회",
      "symposium": "심포지움",
      "conference": "컨퍼런스"
    },
    "vibe": {
      "label": "이벤트 성향"
    }
  },
  "subscription": {
    "created": "구독 생성됨 — 마이페이지 > 구독 탭에서 관리",
    "deleted": "구독이 취소됐어요.",
    "loginRequired": "구독하려면 로그인이 필요해요."
  }
}
```

- [ ] **Step 2A-3: ko/community.json 생성** (이슈 7 — translateAnother 키 포함)

```json
{
  "shell": {
    "title": "커뮤니티",
    "creditsLabel": "크레딧 {{count}}개",
    "creditsPlaceholder": "크레딧",
    "languageToggle": "언어 변경",
    "chatRoomBtn": "채팅방"
  },
  "category": {
    "festival_story": "축제 이야기",
    "mate_finder": "메이트 찾기",
    "free": "자유게시판",
    "all": "전체"
  },
  "post": {
    "loading": "불러오는 중…",
    "notFound": "존재하지 않거나 만료된 게시글이에요.",
    "loadError": "불러오지 못했어요.",
    "likeAriaPressed": "좋아요 취소",
    "likeAriaUnpressed": "좋아요",
    "likeButton": "♥ {{count}}",
    "commentCount": "댓글 ({{count}})",
    "commentSection": "댓글",
    "loginToComment": "로그인 후 댓글을 남길 수 있어요.",
    "deleteConfirm": "게시글을 삭제할까요?",
    "deleteSuccess": "삭제했어요.",
    "deleteForbidden": "본인 글이 아니에요.",
    "deleteFail": "삭제하지 못했어요.",
    "translateTitle": "번역 결과",
    "translateLoading": "번역 중…",
    "translateError": "번역하지 못했어요.",
    "translateClose": "닫기",
    "translateSelectLang": "번역할 언어를 선택하세요",
    "translateOriginal": "원문",
    "translateResult": "번역",
    "translateAnother": "다른 언어로 번역"
  },
  "compose": {
    "newPost": "새 글 작성",
    "editPost": "글 수정",
    "titlePlaceholder": "제목을 입력하세요",
    "bodyPlaceholder": "내용을 입력하세요",
    "categoryLabel": "카테고리",
    "submit": "등록",
    "save": "저장",
    "cancel": "취소",
    "titleRequired": "제목은 2~200자로 입력해 주세요.",
    "bodyRequired": "내용은 2~5000자로 입력해 주세요.",
    "categoryRequired": "카테고리를 선택해 주세요.",
    "loginRequired": "로그인이 필요해요.",
    "submitError": "등록하지 못했어요."
  },
  "comment": {
    "placeholder": "댓글을 입력하세요…",
    "replyPlaceholder": "대댓글을 입력하세요…",
    "submit": "등록",
    "reply": "답글",
    "edit": "수정",
    "delete": "삭제",
    "deleteConfirm": "댓글을 삭제할까요?",
    "deleteForbidden": "본인 댓글이 아니에요.",
    "deleteFail": "삭제하지 못했어요.",
    "replyNotAllowed": "대댓글에는 답글을 달 수 없어요.",
    "bodyLength": "댓글은 1~1000자로 입력해 주세요."
  },
  "authorModal": {
    "title": "{{nickname}} 님",
    "mateScore": "메이트 지수",
    "chatRequest": "채팅 신청",
    "close": "닫기"
  },
  "mateReco": {
    "title": "메이트 추천",
    "placeholder": "로그인 후 메이트를 추천받아요."
  },
  "postList": {
    "empty": "게시글이 없어요.",
    "loadError": "게시글을 불러오지 못했어요.",
    "commentCount": "댓글 {{count}}",
    "likeCount": "♥ {{count}}"
  }
}
```

- [ ] **Step 2A-4: Header.tsx, Sidebar.tsx 하드코딩 → t() 교체**

각 파일 상단에 `import { useTranslation } from 'react-i18next';` 추가.

**Header.tsx** (`AuthArea` 함수):
```tsx
const { t } = useTranslation('common');
```
교체: `"Admin"` → `{t('label.admin')}`, `"Uploader"` → `{t('label.uploader')}`,
`aria-label="관리자 콘솔"` → `aria-label={t('aria.adminConsole')}`,
`aria-label="업로더 콘솔"` → `aria-label={t('aria.uploaderConsole')}`,
`aria-label="마이페이지"` → `aria-label={t('aria.myPage')}`,
`"로그아웃"` → `{t('button.logout')}`,
`"Kakao"` → `{t('label.login_kakao')}`,
`"Google 로그인"` → `{t('label.login_google')}`,
`"Google"` → `{t('label.login_google_short')}`,
`title="빠른 검색..."` → `title={t('aria.quickSearch')}`,
`"이벤트·장소 검색"` → `{t('search.placeholder')}`

**Sidebar.tsx** (`Sidebar` 함수 및 `StatsBlock`):
```tsx
const { t } = useTranslation('navigation');
```
`SECTIONS` 배열을 함수 내부로 이동하여 동적 t() 사용.
eyebrow h2 → `{t('sidebar.eyebrow')}`, description → `{t('sidebar.description')}`.
StatsBlock의 `"현재 공개 이벤트"` / `"전체"` / `"진행중"` / `"예정"` → `t('stats.*')` 교체.

- [ ] **Step 2A-5: MobileShell.tsx 탭 라벨 + 안내 문자열 키화** (이슈 2/10)

`apps/web/src/layout/MobileShell.tsx`:
```tsx
import { useTranslation } from 'react-i18next';
// MobileShell 함수 내부:
const { t } = useTranslation('navigation');
```

`TAB_ORDER` 정적 배열 → 함수 내부 동적 생성:
```tsx
const TAB_ORDER = [
  { key: 'list'   as MobileTab, label: t('mobile.tabs.list'),   icon: 'list'   as const },
  { key: 'filter' as MobileTab, label: t('mobile.tabs.filter'), icon: 'filter' as const },
  { key: 'chat'   as MobileTab, label: t('mobile.tabs.chat'),   icon: 'chat'   as const },
];
```

교체 대상:
- `"목록으로"` → `{t('mobile.backToList')}`
- `"이벤트 요약"` → `{t('mobile.eventSummary')}`
- `aria-label="마이페이지"` → `aria-label={t('common:aria.myPage')}`
- `"로그인"` → `{t('common:button.login')}`
- 채팅 안내 문자열, placeholder, aria-label → `t('mobile.*')` 교체
- `"AI 후보 ${items.length}건 · 의미 기반"` → `` {t('mobile.aiCandidates', { count: items.length })} ``

- [ ] **Step 2A-6: BottomSheet.tsx aria-label 키화** (이슈 2/10)

`apps/web/src/components/mobile/BottomSheet.tsx`:
```tsx
import { useTranslation } from 'react-i18next';
// BottomSheet 함수 내부:
const { t } = useTranslation('common');
```

교체:
- `aria-label={ariaLabel ?? '이벤트 시트'}` → `aria-label={ariaLabel ?? t('aria.eventSheet')}`
- `'시트 절반으로 축소'` → `t('aria.sheetCollapse')`
- `'시트 전체 펼치기'` → `t('aria.sheetExpandFull')`
- `'시트 펼치기'` → `t('aria.sheetExpand')`

- [ ] **Step 2A-7: CommunityShell.tsx, CommunityPage/index.tsx, parts/*.tsx 키화**

**CommunityShell.tsx**: `const { t } = useTranslation('community');`
- `"커뮤니티"` → `{t('shell.title')}`, 크레딧 표시 → `{t('shell.creditsLabel', { count })}`,
- disabled ActionButton → Task 5에서 LanguageToggle로 교체 (여기선 title만 `t('shell.languageToggle')`),
- `"채팅방"` → `{t('shell.chatRoomBtn')}`, `"알림"` → `{t('common:label.notifications')}`

**CategoryGrid.tsx**: `CATEGORY_LABELS` → `t('community:category.*')` 동적 매핑.

**ComposeModal.tsx**: `const { t } = useTranslation('community');` → `t('compose.*')` 교체.

**PostList.tsx**: `const { t } = useTranslation('community');` → `t('postList.*')` 교체.

- [ ] **Step 2A-8: PostDetailPage/index.tsx + parts 키화** (이슈 7 — translateAnother 포함)

`PostDetailPage/index.tsx`: `const { t } = useTranslation('community');`
- 모든 하드코딩 → `t('post.*')` 교체.
- 번역 모달의 `"다른 언어로 번역"` 하드코딩 → `{t('post.translateAnother')}` (이슈 7).

**CommentComposer.tsx**: `t('community:comment.placeholder')` 등 comment 키 사용.

**CommentTree.tsx**: reply/edit/delete 버튼, 삭제 confirm → `t('community:comment.*')`.

**AuthorProfileModal.tsx**: `t('community:authorModal.*')` 교체.

- [ ] **Step 2A-9: FilterSearchPanel.tsx, EventList.tsx, SeoulMap.tsx, EventSummaryPanel.tsx** (이슈 3/11)

각 파일에 `import { useTranslation } from 'react-i18next';` 추가.
`const { t } = useTranslation('navigation');`

**FilterSearchPanel.tsx**:
- 필터 5종 라벨 → `t('filter.region.label')` 등
- 지역 옵션: `"서울"` → `t('filter.region.seoul')`, `"전체"` → `t('filter.region.all')`
- 기간 칩: `"3개월"` → `t('filter.period.3m')` 등
- 인원구성: `"혼자"` → `t('filter.companion.solo')` 등
- 이벤트 종류: `"축제"` → `t('filter.eventType.festival')` 등
- 성향 라벨: `t('filter.vibe.label')`
- 구독 토스트: `t('subscription.created')` / `t('subscription.deleted')` / `t('subscription.loginRequired')`

**EventList.tsx**, **SeoulMap.tsx**, **EventSummaryPanel.tsx**: 각 파일의 한국어 하드코딩 → 적절한 `t()` 키로 교체.

- [ ] **Step 2A-10: FullListPanel.tsx 키화**

`const { t } = useTranslation(['navigation', 'common']);`
한국어 문자열 → `t('common:label.loading')` / `t('common:label.empty')` 등 교체.

- [ ] **Step 2A-11: typecheck**

```bash
cd apps/web && pnpm typecheck
```

- [ ] **Step 2A-12: 커밋**

```bash
git add apps/web/public/locales/ko/common.json apps/web/public/locales/ko/navigation.json apps/web/public/locales/ko/community.json apps/web/src/layout/ apps/web/src/components/mobile/ apps/web/src/pages/CommunityPage apps/web/src/pages/PostDetailPage apps/web/src/components/FilterSearchPanel.tsx apps/web/src/components/EventList.tsx apps/web/src/components/SeoulMap.tsx apps/web/src/components/EventSummaryPanel.tsx apps/web/src/components/FullListPanel.tsx
git commit -m "feat(web): i18n 키화 — common/navigation/community + 모바일 셸 + 필터 5종 (ko 원본)"
```

---

## Task 2-B: ko 원본 — mate / chat / uploader + 해당 화면 키화

**화면군:** MateFormPage(parts 포함), MateRecommendationsPage, ChatRequestPage, ChatRoomPage, ChatDock, ChatHelpPanel, UploaderPage, UploaderNewEventPage, UploaderEventEditPage, EventFormFields, DocumentsPickerField, PosterPickerField

**Files:**
- Create: `apps/web/public/locales/ko/mate.json`
- Create: `apps/web/public/locales/ko/chat.json`
- Create: `apps/web/public/locales/ko/uploader.json`
- Modify: 해당 페이지/컴포넌트 전부

- [ ] **Step 2B-1: ko/mate.json 생성**

```json
{
  "form": {
    "title": "메이트 프로필",
    "subtitle": "함께 즐길 메이트를 찾아보세요",
    "companionType": "동행 유형",
    "eventTypes": "관심 이벤트",
    "vibes": "선호 성향",
    "region": "선호 지역",
    "bio": "자기소개",
    "bioPlaceholder": "간단한 자기소개를 입력하세요 (최대 200자)",
    "submit": "프로필 저장",
    "saved": "저장됐어요.",
    "saveError": "저장하지 못했어요.",
    "loginRequired": "로그인이 필요해요.",
    "consentTitle": "메이트 서비스 이용 동의",
    "consentBody": "메이트 서비스는 다른 사용자와 연결해 줍니다. 개인정보 처리방침에 동의합니다.",
    "consentAgree": "동의하고 계속",
    "consentDecline": "취소"
  },
  "reco": {
    "title": "메이트 추천",
    "loading": "추천 목록을 불러오는 중…",
    "empty": "추천할 메이트가 없어요. 프로필을 채우면 더 잘 매칭돼요.",
    "loadError": "불러오지 못했어요.",
    "chatRequest": "채팅 신청",
    "groupRequest": "그룹 신청",
    "mateScore": "메이트 지수",
    "region": "지역",
    "companions": "동행 유형",
    "vibes": "성향"
  },
  "safetyNotice": {
    "title": "안전 안내",
    "body": "개인정보 보호를 위해 실명·연락처는 공유하지 마세요."
  },
  "authorModal": {
    "mateScore": "메이트 지수",
    "chatRequest": "채팅 신청",
    "close": "닫기"
  }
}
```

- [ ] **Step 2B-2: ko/chat.json 생성**

```json
{
  "dock": {
    "placeholder": "자연어로 이벤트를 찾아보세요…",
    "submit": "전송",
    "collapse": "접기",
    "expand": "펼치기",
    "helpTitle": "채팅 검색 도움말",
    "examples": [
      "이번 주말 가족과 갈 수 있는 축제 알려줘",
      "강남 근처 무료 전시회",
      "혼자 가기 좋은 심포지움"
    ]
  },
  "request": {
    "title": "채팅 신청",
    "typeOneToOne": "1:1 신청",
    "typeGroup": "그룹 신청",
    "message": "메시지",
    "messagePlaceholder": "신청 메시지를 입력하세요",
    "submit": "신청하기",
    "cancel": "취소",
    "loginRequired": "로그인이 필요해요.",
    "submitError": "신청하지 못했어요.",
    "success": "신청했어요."
  },
  "room": {
    "loading": "불러오는 중…",
    "loadError": "채팅방을 불러오지 못했어요.",
    "inputPlaceholder": "메시지를 입력하세요…",
    "send": "전송",
    "leave": "나가기",
    "leaveConfirm": "채팅방을 나갈까요?",
    "leaveFail": "나가기를 실패했어요.",
    "block": "차단",
    "blockConfirm": "이 사용자를 차단할까요?",
    "blockFail": "차단하지 못했어요.",
    "selectEvent": "이벤트 선택",
    "proposeAppointment": "약속 제안",
    "voteYes": "수락",
    "voteNo": "거절",
    "memberCount": "{{count}}명",
    "kick": "내보내기",
    "kickVote": "투표로 내보내기"
  }
}
```

- [ ] **Step 2B-3: ko/uploader.json 생성** (이슈 3/12 — EventFormFields enum 옵션 라벨 포함)

```json
{
  "page": {
    "title": "업로더 콘솔",
    "myEvents": "내 이벤트",
    "newEvent": "이벤트 등록",
    "apply": "업로더 신청",
    "applyDescription": "업로더 신청을 해야 이벤트를 등록할 수 있어요.",
    "pendingApproval": "승인 대기 중이에요.",
    "status": "상태"
  },
  "form": {
    "title": "제목",
    "titlePlaceholder": "이벤트 제목을 입력하세요",
    "description": "설명",
    "descriptionPlaceholder": "이벤트 설명을 입력하세요",
    "category": "카테고리",
    "startDate": "시작일",
    "endDate": "종료일",
    "region": "지역",
    "venue": "장소",
    "venuePlaceholder": "장소명을 입력하세요",
    "poster": "포스터",
    "posterHint": "대표 이미지를 업로드하세요 (최대 5MB, JPG/PNG)",
    "document": "첨부 서류",
    "documentHint": "신원 확인 서류를 첨부하세요 (최대 10MB)",
    "submit": "등록 신청",
    "save": "저장",
    "cancel": "취소",
    "submitSuccess": "등록 신청됐어요.",
    "submitError": "등록하지 못했어요.",
    "saveSuccess": "저장됐어요.",
    "saveError": "저장하지 못했어요.",
    "uploading": "업로드 중…",
    "uploadError": "업로드하지 못했어요."
  },
  "category": {
    "festival": "축제",
    "expo": "박람회",
    "symposium": "심포지움",
    "conference": "컨퍼런스"
  },
  "companion": {
    "solo": "혼자",
    "couple": "커플",
    "family": "가족",
    "friend": "친구"
  },
  "status": {
    "pending": "검토 중",
    "approved": "승인됨",
    "rejected": "반려됨",
    "revision_requested": "수정 요청",
    "ended": "종료됨"
  }
}
```

- [ ] **Step 2B-4: MateFormPage, MateRecommendationsPage, ChatRequestPage, ChatRoomPage, ChatDock, ChatHelpPanel 키화**

각 파일 상단 `import { useTranslation } from 'react-i18next';` 추가.

`MateFormPage/index.tsx` + `parts/*.tsx`: `useTranslation('mate')` → `t('form.*')` / `t('safetyNotice.*')` 교체.

`MateRecommendationsPage/index.tsx`: `useTranslation('mate')` → `t('reco.*')` 교체.

`ChatRequestPage/index.tsx`: `useTranslation('chat')` → `t('request.*')` 교체.

`ChatRoomPage/index.tsx`: `useTranslation('chat')` → `t('room.*')` 교체.

`ChatDock.tsx`: `useTranslation('chat')` → `t('dock.*')` 교체.

`ChatHelpPanel.tsx`: `useTranslation('chat')` → `t('dock.helpTitle')` 등 교체.

- [ ] **Step 2B-5: UploaderPage, UploaderNewEventPage, UploaderEventEditPage, EventFormFields, DocumentsPickerField, PosterPickerField 키화** (이슈 3/12)

각 파일: `useTranslation('uploader')` → `t('page.*')` / `t('form.*')` / `t('status.*')` 교체.

**EventFormFields.tsx**: 카테고리 enum 옵션 라벨 → `t('uploader:category.*')`, 동행 라벨 → `t('uploader:companion.*')`.

**DocumentsPickerField.tsx**: 안내 문자열 → `t('uploader:form.documentHint')` / `t('uploader:form.uploading')` 등.

**PosterPickerField.tsx**: 안내 문자열 → `t('uploader:form.posterHint')` / `t('uploader:form.uploading')` 등.

- [ ] **Step 2B-6: typecheck**

```bash
cd apps/web && pnpm typecheck
```

- [ ] **Step 2B-7: 커밋**

```bash
git add apps/web/public/locales/ko/mate.json apps/web/public/locales/ko/chat.json apps/web/public/locales/ko/uploader.json apps/web/src/pages/MateFormPage apps/web/src/pages/MateRecommendationsPage apps/web/src/pages/ChatRequestPage apps/web/src/pages/ChatRoomPage apps/web/src/components/ChatDock.tsx apps/web/src/components/ChatHelpPanel.tsx apps/web/src/pages/UploaderPage.tsx apps/web/src/pages/UploaderNewEventPage.tsx apps/web/src/pages/UploaderEventEditPage.tsx apps/web/src/components/uploader/
git commit -m "feat(web): i18n 키화 — mate/chat/uploader + EventFormFields enum 라벨 (ko 원본)"
```

---

## Task 2-C: ko 원본 — admin / mypage + 해당 화면 키화 (전체 패널 포함)

**화면군:** AdminEventsPage(탭), components/admin/*(패널 전체), MyPage(탭), EvaluationPage, CreditPage, ProfilePage, NotificationsPage, EventDetailPage

**Files:**
- Create: `apps/web/public/locales/ko/admin.json`
- Create: `apps/web/public/locales/ko/mypage.json`
- Modify (이슈 1/9 — detail-level 패널 전부 포함):
  - `apps/web/src/pages/AdminEventsPage/index.tsx` + `tabs/*.tsx`
  - `apps/web/src/components/admin/MembersTab.tsx`
  - `apps/web/src/components/admin/AuditLogsTab.tsx`
  - `apps/web/src/components/admin/UploadReviewPanel.tsx`
  - `apps/web/src/components/admin/UserDetailPanel.tsx`
  - `apps/web/src/components/admin/UploaderDetailPanel.tsx`
  - `apps/web/src/components/admin/audit/AuditDashboard.tsx`
  - `apps/web/src/pages/MyPage/index.tsx` + `tabs/*.tsx`
  - `apps/web/src/pages/EvaluationPage/index.tsx` + `parts/*.tsx`
  - `apps/web/src/pages/CreditPage/index.tsx`
  - `apps/web/src/pages/ProfilePage/index.tsx`
  - `apps/web/src/pages/NotificationsPage.tsx`
  - `apps/web/src/pages/EventDetailPage/index.tsx` + `parts/*.tsx`
  - `apps/web/src/components/ReportModal.tsx`

- [ ] **Step 2C-1: ko/admin.json 생성** (이슈 1/9 — detail-level 키 전부 포함)

```json
{
  "page": { "title": "관리자 콘솔" },
  "tabs": {
    "events": "이벤트 심사",
    "uploaders": "업로더 심사",
    "uploadReviews": "업로드 검토",
    "reports": "신고 관리",
    "members": "회원 관리",
    "audit": "감사 로그"
  },
  "event": {
    "approve": "승인",
    "reject": "반려",
    "requestRevision": "수정 요청",
    "assignVibes": "성향 라벨 부여",
    "status": {
      "pending": "검토 중",
      "approved": "승인됨",
      "rejected": "반려됨",
      "revision_requested": "수정 요청",
      "ended": "종료됨"
    },
    "empty": "심사할 이벤트가 없어요.",
    "loadError": "불러오지 못했어요."
  },
  "uploader": {
    "approve": "승인",
    "reject": "반려",
    "requestRevision": "보완요청",
    "loading": "불러오는 중…",
    "loadError": "조회 실패",
    "empty": "심사할 업로더 신청이 없어요.",
    "nickname": "닉네임",
    "joinMethod": "가입 경로",
    "email": "이메일",
    "contact": "연락처",
    "createdAt": "계정 생성",
    "appliedAt": "신청",
    "approvedAt": "승인",
    "identity": "신원 정보",
    "realName": "실명",
    "businessNo": "사업자번호",
    "idVerified": "본인인증",
    "documents": "제출 서류",
    "eventStats": "등록 이벤트 현황",
    "reasonLabel": "결정 사유 (선택)",
    "reasonPlaceholder": "보완요청/반려 사유를 업로더에게 전달하려면 작성",
    "notRegistered": "(미등록)",
    "maskingNote": "(마스킹 · scope={{scope}})"
  },
  "uploadReview": {
    "documents": "제출 서류",
    "loading": "서류 불러오는 중…",
    "noDocuments": "첨부된 서류가 없어요.",
    "reasonLabel": "결정 사유 (선택)",
    "reasonPlaceholder": "보완요청/반려 사유를 업로더에게 전달하려면 작성",
    "reject": "반려",
    "requestRevision": "보완요청",
    "approve": "승인"
  },
  "report": {
    "action": "조치",
    "dismiss": "기각",
    "warn": "경고",
    "suspend": "정지",
    "empty": "신고가 없어요.",
    "loadError": "불러오지 못했어요."
  },
  "member": {
    "searchPlaceholder": "닉네임 검색",
    "roleFilter": {
      "all": "전체",
      "general": "일반",
      "uploader": "업로더",
      "admin": "admin"
    },
    "statusFilter": {
      "active": "활성",
      "deleted": "삭제됨",
      "all": "전체"
    },
    "empty": "회원이 없어요.",
    "loadError": "불러오지 못했어요.",
    "revokeSession": "세션 강제 폐기",
    "softDelete": "계정 비활성화",
    "promoteAdmin": "admin 승급",
    "demoteAdmin": "admin 박탈",
    "changeScope": "admin scope 변경",
    "uploaderDecision": "업로더 심사",
    "actionLabel": {
      "revoke_sessions": "세션 강제 폐기",
      "admin_promote": "admin 승급",
      "admin_demote": "admin 박탈",
      "admin_scope_change": "admin scope 변경",
      "user_soft_delete": "계정 비활성화",
      "uploader_decision": "업로더 심사"
    },
    "scopeLabel": {
      "full": "full (전체 권한)",
      "content_only": "content_only",
      "uploader_review_only": "uploader_review_only",
      "security": "security (세션 폐기 전용)"
    },
    "uploaderStatus": {
      "pending": "대기",
      "approved": "승인됨",
      "revision_requested": "보완요청",
      "rejected": "반려"
    },
    "reasonLabel": "사유 (10~500자)",
    "reasonPlaceholder": "액션 사유를 입력하세요",
    "confirmRevoke": "세션을 강제 폐기할까요?",
    "confirmSoftDelete": "계정을 비활성화할까요? 복구 불가.",
    "confirmPromote": "admin으로 승급할까요?",
    "confirmDemote": "admin을 박탈할까요?",
    "confirmScopeChange": "scope를 변경할까요?"
  },
  "audit": {
    "overview": "개요",
    "eventSource": "이벤트 심사",
    "adminSource": "admin 액션",
    "empty": "로그가 없어요.",
    "loadError": "불러오지 못했어요.",
    "eventAction": {
      "any": "전체",
      "approved": "승인",
      "revision_requested": "보완 요청",
      "rejected": "반려"
    },
    "adminAction": {
      "revoke_sessions": "세션 폐기",
      "admin_promote": "admin 승급",
      "admin_demote": "admin 박탈",
      "admin_scope_change": "scope 변경",
      "user_soft_delete": "계정 비활성화",
      "uploader_decision": "업로더 심사"
    }
  }
}
```

- [ ] **Step 2C-2: ko/mypage.json 생성**

```json
{
  "page": { "title": "마이페이지" },
  "tabs": {
    "bookmarks": "북마크",
    "reviews": "내 리뷰",
    "recommendations": "추천",
    "subscriptions": "구독",
    "calendar": "캘린더"
  },
  "bookmark": {
    "empty": "북마크한 이벤트가 없어요.",
    "loadError": "불러오지 못했어요."
  },
  "review": {
    "empty": "작성한 리뷰가 없어요.",
    "loadError": "불러오지 못했어요.",
    "delete": "삭제",
    "deleteConfirm": "리뷰를 삭제할까요?"
  },
  "reco": {
    "empty": "추천 이벤트가 없어요.",
    "loadError": "불러오지 못했어요."
  },
  "subscription": {
    "empty": "구독한 이벤트가 없어요.",
    "loadError": "불러오지 못했어요.",
    "toggleOn": "알림 켜짐",
    "toggleOff": "알림 꺼짐",
    "delete": "구독 취소"
  },
  "calendar": {
    "empty": "예정된 약속이 없어요.",
    "loadError": "불러오지 못했어요.",
    "evaluate": "평가하기"
  },
  "credit": {
    "title": "크레딧 내역",
    "balance": "잔액: {{count}}개",
    "empty": "내역이 없어요.",
    "loadError": "불러오지 못했어요."
  },
  "profile": {
    "title": "프로필 수정",
    "nickname": "닉네임",
    "nicknamePlaceholder": "닉네임을 입력하세요",
    "save": "저장",
    "saveSuccess": "저장됐어요.",
    "saveError": "저장하지 못했어요."
  },
  "notification": {
    "title": "알림",
    "empty": "알림이 없어요.",
    "loadError": "불러오지 못했어요.",
    "markAllRead": "모두 읽음",
    "markRead": "읽음"
  },
  "evaluation": {
    "title": "평가",
    "mateEval": "메이트 평가",
    "festivalEval": "축제 후기",
    "submit": "제출",
    "submitSuccess": "평가했어요.",
    "submitError": "제출하지 못했어요.",
    "alreadyEvaluated": "이미 평가했어요."
  },
  "role": {
    "toggle": "역할 전환",
    "user": "일반 사용자",
    "uploader": "업로더",
    "admin": "관리자",
    "switchToUploader": "업로더 모드",
    "switchToUser": "사용자 모드"
  }
}
```

- [ ] **Step 2C-3: AdminEventsPage 탭 + components/admin/* 패널 전체 키화** (이슈 1/9)

`AdminEventsPage/index.tsx` 및 `tabs/*.tsx`:
```tsx
const { t } = useTranslation('admin');
```
모든 하드코딩 → `t('page.*')` / `t('tabs.*')` / `t('event.*')` 등 교체.

**components/admin/MembersTab.tsx**:
```tsx
const { t } = useTranslation('admin');
```
`ROLE_FILTERS`/`STATUS_FILTERS` 라벨 → `t('member.roleFilter.*')` / `t('member.statusFilter.*')`.
검색 placeholder → `t('member.searchPlaceholder')`.

**components/admin/UserDetailPanel.tsx** (reason textarea, scope 라벨, 액션 라벨, 상태 라벨 전부):
```tsx
const { t } = useTranslation('admin');
```
`SCOPE_LABEL` → `t('member.scopeLabel.*')`.
`UPLOADER_STATUS_KO` → `t('member.uploaderStatus.*')`.
`ACTION_LABEL_KO` → `t('member.actionLabel.*')`.
reason textarea label/placeholder → `t('member.reasonLabel')` / `t('member.reasonPlaceholder')`.
confirm 문자열 → `t('member.confirmRevoke')` 등.

**components/admin/UploaderDetailPanel.tsx**:
```tsx
const { t } = useTranslation('admin');
```
상태 상수 → `t('uploader.uploaderStatus.*')` 또는 `t('member.uploaderStatus.*')`.
닉네임/가입경로/이메일 등 dt 라벨 → `t('uploader.*')`.
서류/이벤트 현황 헤딩 → `t('uploader.documents')` / `t('uploader.eventStats')`.
reason label/placeholder → `t('uploader.reasonLabel')` / `t('uploader.reasonPlaceholder')`.

**components/admin/UploadReviewPanel.tsx**:
```tsx
const { t } = useTranslation('admin');
```
`"제출 서류"` → `t('uploadReview.documents')`.
`"서류 불러오는 중…"` → `t('uploadReview.loading')`.
`"첨부된 서류가 없어요."` → `t('uploadReview.noDocuments')`.
`"결정 사유 (선택)"` → `t('uploadReview.reasonLabel')`.
placeholder → `t('uploadReview.reasonPlaceholder')`.
`"반려"` / `"보완요청"` / `"승인"` 버튼 → `t('uploadReview.*')`.

**components/admin/AuditLogsTab.tsx**:
```tsx
const { t } = useTranslation('admin');
```
`EVENT_ACTION_LABEL` → `t('audit.eventAction.*')`.
`ADMIN_ACTION_LABEL` → `t('audit.adminAction.*')`.
source 탭 라벨 → `t('audit.overview')` / `t('audit.eventSource')` / `t('audit.adminSource')`.

**components/admin/audit/AuditDashboard.tsx**:
```tsx
const { t } = useTranslation('admin');
```
대시보드 내 한국어 문자열 전부 → `t('audit.*')` 교체.

- [ ] **Step 2C-4: MyPage, EvaluationPage, CreditPage, ProfilePage, NotificationsPage 키화**

`MyPage/index.tsx` + `tabs/*.tsx`:
```tsx
const { t } = useTranslation('mypage');
```
탭 라벨, 각 탭 빈 상태/오류 메시지 → `t('tabs.*')` / `t('bookmark.*')` 등 교체.

`EvaluationPage/index.tsx` + `parts/FestivalStep.tsx` + `parts/MateEvalStep.tsx`:
`t('mypage:evaluation.*')` 교체.

`CreditPage/index.tsx`: `t('mypage:credit.*')` 교체.

`ProfilePage/index.tsx`: `t('mypage:profile.*')` 교체.

`NotificationsPage.tsx`: `t('mypage:notification.*')` 교체.

- [ ] **Step 2C-5: EventDetailPage + ReportModal 키화**

`EventDetailPage/index.tsx` + `parts/*.tsx`:
```tsx
const { t } = useTranslation(['common', 'mypage']);
```
이벤트 상세 한국어 문자열 → `t('common:*')` / `t('mypage:review.*')` 교체.

`ReportModal.tsx`: `useTranslation('common')` → `t('button.report')` 등 교체.

- [ ] **Step 2C-6: typecheck + 빌드**

```bash
cd apps/web && pnpm typecheck && pnpm build
```

Expected: TypeScript 오류 없음, 빌드 성공.

- [ ] **Step 2C-7: 커밋**

```bash
git add apps/web/public/locales/ko/admin.json apps/web/public/locales/ko/mypage.json apps/web/src/pages/AdminEventsPage apps/web/src/components/admin/ apps/web/src/pages/MyPage apps/web/src/pages/EvaluationPage apps/web/src/pages/CreditPage apps/web/src/pages/ProfilePage apps/web/src/pages/NotificationsPage.tsx apps/web/src/pages/EventDetailPage apps/web/src/components/ReportModal.tsx
git commit -m "feat(web): i18n 키화 완료 — admin 패널 전체(detail-level) + mypage (ko 원본)"
```

---

## Task 3: 6개국어 번역 번들 생성 (en/vi/zh/ja/fr)

**Files:**
- Create: `services/llm/translate.py`
- Modify: `services/llm/app.py`
- Create: `apps/bff/src/jobs/generate-i18n-bundles.ts`
- Create: `apps/web/public/locales/{en,vi,zh,ja,fr}/*.json` (5×8=40개)

- [ ] **Step 3-1: services/llm/translate.py 생성**

Create `services/llm/translate.py`:

```python
"""
Alle LLM — 번역 엔드포인트.

POST /translate-bundle  body: {namespace, lang, keys}  reply: {translated}
POST /translate-post    body: {content, target_lang}    reply: {translated}
"""
from __future__ import annotations
import os
import json as _json
from typing import Any

LANGUAGE_NAMES = {
    "en": "English",
    "vi": "Vietnamese",
    "zh": "Simplified Chinese",
    "ja": "Japanese",
    "fr": "French",
}


def _openai_client():
    from openai import OpenAI
    return OpenAI(api_key=os.environ["OPENAI_API_KEY"])


def translate_bundle(namespace: str, lang: str, keys: dict[str, Any]) -> dict[str, Any]:
    """네임스페이스 JSON 전체를 한 번의 OpenAI 호출로 번역. 실패 시 예외 올림."""
    lang_name = LANGUAGE_NAMES.get(lang, lang)
    client = _openai_client()
    prompt_json = _json.dumps(keys, ensure_ascii=False, indent=2)
    resp = client.chat.completions.create(
        model="gpt-4o-mini",
        messages=[
            {
                "role": "system",
                "content": (
                    f"You are a professional UI translator for a Korean event discovery app called Alle. "
                    f"Translate the JSON object's string VALUES from Korean to {lang_name}. "
                    "Rules:\n"
                    "- Preserve all JSON keys exactly as-is.\n"
                    "- Preserve {{variable}} interpolation placeholders exactly.\n"
                    "- Keep the same JSON structure.\n"
                    "- Return ONLY valid JSON, no markdown fences, no explanation.\n"
                    "- Translate naturally for a mobile UI context (short, clear labels)."
                ),
            },
            {"role": "user", "content": prompt_json},
        ],
        temperature=0.1,
        max_tokens=8000,
    )
    raw = (resp.choices[0].message.content or "").strip()
    # 마크다운 코드 펜스 제거
    if raw.startswith("```"):
        raw = raw.split("```", 2)[1]
        if raw.startswith("json"):
            raw = raw[4:]
        raw = raw.rsplit("```", 1)[0].strip()
    return _json.loads(raw)


def translate_post_content(content: str, target_lang: str) -> str:
    """게시글 본문 단일 텍스트 번역. 실패 시 예외 올림."""
    lang_name = LANGUAGE_NAMES.get(target_lang, target_lang)
    client = _openai_client()
    resp = client.chat.completions.create(
        model="gpt-4o-mini",
        messages=[
            {
                "role": "system",
                "content": (
                    f"You are a professional translator. "
                    f"Translate the user's Korean text to {lang_name}. "
                    "Preserve {{placeholders}} exactly. "
                    "Return only the translated text, no explanation."
                ),
            },
            {"role": "user", "content": content},
        ],
        temperature=0.2,
        max_tokens=4000,
    )
    return (resp.choices[0].message.content or "").strip()
```

- [ ] **Step 3-2: services/llm/app.py에 번역 엔드포인트 append** (ChatSession/chat-eval 수정 금지)

`services/llm/app.py` 파일 맨 끝에 다음을 추가 (기존 코드 수정 없음):

```python
# ── Slice 7: 번역 엔드포인트 ────────────────────────────────────────────────
from pydantic import BaseModel, Field as PydField


class TranslateBundleRequest(BaseModel):
    namespace: str = PydField(max_length=50)
    lang: str = PydField(pattern="^(en|vi|zh|ja|fr)$")
    keys: dict


class TranslateBundleResponse(BaseModel):
    namespace: str
    lang: str
    translated: dict


class TranslatePostRequest(BaseModel):
    content: str = PydField(min_length=1, max_length=10000)
    target_lang: str = PydField(pattern="^(en|vi|zh|ja|fr)$")


class TranslatePostResponse(BaseModel):
    translated: str


@app.post("/translate-bundle", response_model=TranslateBundleResponse)
def translate_bundle_endpoint(req: TranslateBundleRequest):
    from fastapi import HTTPException
    if not _openai_available():
        raise HTTPException(status_code=503, detail="translation unavailable")
    try:
        from translate import translate_bundle
        result = translate_bundle(req.namespace, req.lang, req.keys)
    except Exception as e:
        raise HTTPException(status_code=502, detail=f"translation failed: {type(e).__name__}: {e}")
    return TranslateBundleResponse(namespace=req.namespace, lang=req.lang, translated=result)


@app.post("/translate-post", response_model=TranslatePostResponse)
def translate_post_endpoint(req: TranslatePostRequest):
    from fastapi import HTTPException
    if not _openai_available():
        raise HTTPException(status_code=503, detail="translation unavailable")
    try:
        from translate import translate_post_content
        result = translate_post_content(req.content, req.target_lang)
    except Exception as e:
        raise HTTPException(status_code=502, detail=f"translation failed: {type(e).__name__}: {e}")
    return TranslatePostResponse(translated=result)
```

- [ ] **Step 3-3: generate-i18n-bundles.ts 생성** (이슈 8 — 실패 시 재시도/중단, 빈 객체 금지)

Create `apps/bff/src/jobs/generate-i18n-bundles.ts`:

```typescript
/**
 * 빌드타임 i18n 번들 생성 스크립트.
 * 사용법: pnpm i18n:generate
 *
 * ko 원본을 읽어 en/vi/zh/ja/fr 각 언어로 번역. 실패 시 재시도 1회 후 프로세스 종료(빈 객체 금지).
 */
import { readFile, writeFile, mkdir } from 'node:fs/promises';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import { env } from '../env.js';

const __dirname = dirname(fileURLToPath(import.meta.url));
const LOCALES_DIR = join(__dirname, '../../../../apps/web/public/locales');
const NAMESPACES = ['common', 'navigation', 'community', 'mate', 'chat', 'uploader', 'admin', 'mypage'] as const;
const TARGET_LANGS = ['en', 'vi', 'zh', 'ja', 'fr'] as const;
const RETRY = 1;
const DELAY_MS = 1200;

async function translateBundle(namespace: string, lang: string, keys: unknown, attempt = 0): Promise<unknown> {
  const url = `${env.LLM_SERVICE_URL}/translate-bundle`;
  const res = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json; charset=utf-8' },
    body: JSON.stringify({ namespace, lang, keys }),
  });
  if (!res.ok) {
    const text = await res.text().catch(() => '');
    const err = new Error(`LLM /translate-bundle ${res.status}: ${text.slice(0, 200)}`);
    if (attempt < RETRY) {
      console.warn(`  retry ${attempt + 1}/${RETRY} for ${lang}/${namespace}...`);
      await new Promise((r) => setTimeout(r, DELAY_MS * 2));
      return translateBundle(namespace, lang, keys, attempt + 1);
    }
    throw err;
  }
  const data = await res.json() as { translated: unknown };
  return data.translated;
}

async function main() {
  let failures = 0;
  for (const ns of NAMESPACES) {
    const koPath = join(LOCALES_DIR, 'ko', `${ns}.json`);
    let koData: unknown;
    try {
      koData = JSON.parse(await readFile(koPath, 'utf-8'));
    } catch {
      console.warn(`[skip] ko/${ns}.json not found`);
      continue;
    }

    for (const lang of TARGET_LANGS) {
      const outPath = join(LOCALES_DIR, lang, `${ns}.json`);
      console.log(`Translating ${ns} → ${lang}...`);
      try {
        const translated = await translateBundle(ns, lang, koData);
        await mkdir(join(LOCALES_DIR, lang), { recursive: true });
        await writeFile(outPath, JSON.stringify(translated, null, 2) + '\n', 'utf-8');
        console.log(`  ✓ saved ${lang}/${ns}.json`);
      } catch (err) {
        // [이슈 8] 빈 객체 저장 금지 — 실패는 즉시 에러 로그, 최종 집계 후 exit 1
        console.error(`  ✗ FAILED ${lang}/${ns}: ${(err as Error).message}`);
        failures++;
      }
      await new Promise((r) => setTimeout(r, DELAY_MS));
    }
  }
  if (failures > 0) {
    console.error(`\n${failures} translation(s) failed. Fix and re-run.`);
    process.exit(1);
  }
  console.log('All bundles generated successfully.');
}

main().catch((e) => { console.error(e); process.exit(1); });
```

`apps/bff/package.json` scripts에 추가:
```json
"i18n:generate": "dotenv -e ../../.env -- tsx src/jobs/generate-i18n-bundles.ts"
```

- [ ] **Step 3-4: 번역 실행**

LLM 서비스 기동 상태에서:
```bash
cd apps/bff && pnpm i18n:generate
```

Expected: 5×8=40개 JSON 파일 생성. 실패 시 즉시 오류 출력 후 exit 1.

- [ ] **Step 3-5: deep-diff 검증 — ko 대비 누락 키 0 확인** (이슈 8/14)

```bash
node --input-type=module << 'EOF'
import { readFileSync } from 'fs';
import { join } from 'path';

const dir = 'apps/web/public/locales';
const langs = ['en','vi','zh','ja','fr'];
const ns = ['common','navigation','community','mate','chat','uploader','admin','mypage'];

function collectLeafKeys(obj, prefix = '') {
  let keys = [];
  for (const [k, v] of Object.entries(obj)) {
    const path = prefix ? `${prefix}.${k}` : k;
    if (typeof v === 'object' && v !== null && !Array.isArray(v)) {
      keys = keys.concat(collectLeafKeys(v, path));
    } else {
      keys.push(path);
    }
  }
  return keys;
}

let issues = 0;
for (const n of ns) {
  const koKeys = new Set(collectLeafKeys(JSON.parse(readFileSync(join(dir,'ko',`${n}.json`),'utf-8'))));
  for (const lang of langs) {
    const translated = JSON.parse(readFileSync(join(dir,lang,`${n}.json`),'utf-8'));
    const tKeys = new Set(collectLeafKeys(translated));
    const missing = [...koKeys].filter(k => !tKeys.has(k));
    if (missing.length > 0) {
      console.error(`MISSING keys in ${lang}/${n}.json:`, missing);
      issues += missing.length;
    }
  }
}
console.log(issues === 0 ? 'deep-diff OK: 0 missing keys across all 40 bundles.' : `${issues} missing key(s) — fix and re-run.`);
if (issues > 0) process.exit(1);
EOF
```

Expected: `deep-diff OK: 0 missing keys across all 40 bundles.`
누락이 있으면 해당 언어/네임스페이스를 개별 재실행.

- [ ] **Step 3-6: 빌드 확인**

```bash
cd apps/web && pnpm build
```

- [ ] **Step 3-7: 커밋**

```bash
git add services/llm/translate.py services/llm/app.py apps/bff/src/jobs/generate-i18n-bundles.ts apps/bff/package.json apps/web/public/locales/
git commit -m "feat(llm,web): 6개국어 번역 번들 생성 — translate.py + 생성 스크립트 + deep-diff 검증 통과"
```

---

## Task 4: 게시글 번역 BFF 엔드포인트 + Redis 캐시

**Files:**
- Create: `apps/bff/src/lib/translation-cache.ts`
- Create: `apps/bff/src/routes/translate.ts`
- Modify: `apps/bff/src/app.ts`
- `apps/bff/src/lib/redis-client.ts` — **수정 없음** (import 재사용만, 이슈 6)

- [ ] **Step 4-1: translation-cache.ts 생성**

Create `apps/bff/src/lib/translation-cache.ts`:

```typescript
/**
 * 게시글 번역 결과 Redis 캐시 helpers.
 *
 * 캐시 키 prefix: post:translation:{postId}:{targetLang}
 * TTL: 7일 (604800초)
 * 기존 Socket.IO adapter 키(socket.io#*) 및 stream-cache 키와 충돌 없음 — prefix 분리.
 * 무마이그레이션: Redis만 사용, Prisma 스키마 변경 없음. (이슈 22)
 */
import { getRedisClient } from './redis-client.js';

const TTL_SECONDS = 7 * 24 * 60 * 60; // 7d

function cacheKey(postId: string, lang: string): string {
  return `post:translation:${postId}:${lang}`;
}

export async function getTranslationCache(postId: string, lang: string): Promise<string | null> {
  return getRedisClient().get(cacheKey(postId, lang));
}

export async function setTranslationCache(postId: string, lang: string, translated: string): Promise<void> {
  await getRedisClient().set(cacheKey(postId, lang), translated, 'EX', TTL_SECONDS);
}
```

- [ ] **Step 4-2: routes/translate.ts 생성** (이슈 23 — 에러 처리 & 폴백 명시)

Create `apps/bff/src/routes/translate.ts`:

```typescript
import type { Request, Response } from 'express';
import { prisma } from '../prisma.js';
import { callLlm } from '../llm-client.js';
import { getTranslationCache, setTranslationCache } from '../lib/translation-cache.js';

const SUPPORTED_LANGS = new Set(['en', 'vi', 'zh', 'ja', 'fr']);

function parseBigId(raw: unknown): bigint | null {
  try { const n = BigInt(String(raw ?? '')); return n > 0n ? n : null; } catch { return null; }
}

interface LlmTranslateResponse { translated: string }

/**
 * POST /community/posts/:id/translate
 * Request body: { targetLanguage: 'en'|'vi'|'zh'|'ja'|'fr' }
 * Response: { postId, originalBody, translatedBody, targetLanguage, cached }
 *
 * 에러 처리 & 폴백 정책 (이슈 23):
 *  - LLM rate-limit/타임아웃/502 → originalBody 그대로 반환 (graceful degradation)
 *  - Redis 에러 → 캐시 miss로 간주, 번역 계속 진행
 *  - 게시글 없음 → 404
 *  - 인증: 비로그인도 가능 (resolveAuth 사용, requireAuth 아님)
 */
export async function translatePost(req: Request, res: Response): Promise<void> {
  const postId = parseBigId(req.params.id);
  if (!postId) { res.status(400).json({ error: 'invalid id' }); return; }

  const targetLanguage = String((req.body as Record<string, unknown>)?.targetLanguage ?? '');
  if (!SUPPORTED_LANGS.has(targetLanguage)) {
    res.status(400).json({ error: 'unsupported targetLanguage. Use: en, vi, zh, ja, fr' });
    return;
  }

  const post = await prisma.post.findFirst({
    where: { postId, isDeleted: false, expiresAt: { gt: new Date() } },
    select: { postId: true, body: true },
  });
  if (!post) { res.status(404).json({ error: 'post not found' }); return; }

  const postIdStr = post.postId.toString();

  // Redis 캐시 확인 (에러 시 miss로 처리)
  const cached = await getTranslationCache(postIdStr, targetLanguage).catch(() => null);
  if (cached !== null) {
    res.json({ postId: postIdStr, originalBody: post.body, translatedBody: cached, targetLanguage, cached: true });
    return;
  }

  // LLM 호출 — 실패 시 originalBody 폴백 (graceful degradation)
  const llmResult = await callLlm<LlmTranslateResponse>('/translate-post', {
    content: post.body,
    target_lang: targetLanguage,
  }).catch(() => null);

  const translatedBody = llmResult?.translated ?? post.body;
  const wasTranslated = !!llmResult?.translated;

  // 번역 성공 시에만 캐시 저장
  if (wasTranslated) {
    await setTranslationCache(postIdStr, targetLanguage, translatedBody).catch(() => undefined);
  }

  res.json({ postId: postIdStr, originalBody: post.body, translatedBody, targetLanguage, cached: false });
}
```

- [ ] **Step 4-3: app.ts에 translate 라우트 등록** (이슈 19)

`apps/bff/src/app.ts`에 다음 import 추가:
```typescript
import { translatePost } from './routes/translate.js';
```

커뮤니티 posts 라우트 블록 끝에 추가:
```typescript
// GG-COMM-013 게시글 번역 (비로그인 가능 — resolveAuth)
app.post(
  '/community/posts/:id/translate',
  (req, res, next) => resolveAuth(req, res, next).catch(next),
  (req, res, next) => translatePost(req, res).catch(next),
);
```

- [ ] **Step 4-4: BFF typecheck**

```bash
cd apps/bff && pnpm typecheck
```

- [ ] **Step 4-5: 수동 검증**

```bash
# 1회차 — LLM 번역
curl -s -X POST http://localhost:3000/community/posts/1/translate \
  -H "Content-Type: application/json" -d '{"targetLanguage":"en"}' | python3 -m json.tool

# 2회차 — Redis 캐시 hit 확인
curl -s -X POST http://localhost:3000/community/posts/1/translate \
  -H "Content-Type: application/json" -d '{"targetLanguage":"en"}' \
  | python3 -c "import sys,json; print('cached:', json.load(sys.stdin)['cached'])"
```

Expected: 2회차 `cached: True`.

- [ ] **Step 4-6: 커밋**

```bash
git add apps/bff/src/lib/translation-cache.ts apps/bff/src/routes/translate.ts apps/bff/src/app.ts
git commit -m "feat(bff): 게시글 번역 엔드포인트 — Redis TTL 7d 캐시, LLM graceful degradation, 키 prefix 분리"
```

---

## Task 5: 언어 토글 UI 실연결 (GG-COMM-013)

**Files:**
- Create: `apps/web/src/components/LanguageToggle.tsx`
- Modify: `apps/web/src/pages/CommunityPage/parts/CommunityShell.tsx`

- [ ] **Step 5-1: LanguageToggle.tsx 생성**

Create `apps/web/src/components/LanguageToggle.tsx`:

```tsx
import { useState, useRef, useEffect } from 'react';
import { useLanguage } from '../lib/useLanguage.js';
import type { SupportedLanguage } from '../lib/i18n.js';

/**
 * GG-COMM-013 언어 전환 드롭다운.
 * 선택 즉시 i18n.changeLanguage() + localStorage 저장 → 전 서비스 언어 변경.
 * DESIGN.md: neutralOutline 변형, pill 버튼/보라 그라디언트 금지.
 */
export function LanguageToggle() {
  const { current, setLanguage, languages } = useLanguage();
  const [open, setOpen] = useState(false);
  const ref = useRef<HTMLDivElement>(null);

  const currentLang = languages.find((l) => l.code === current) ?? languages[0]!;

  useEffect(() => {
    const handler = (e: MouseEvent) => {
      if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false);
    };
    document.addEventListener('mousedown', handler);
    return () => document.removeEventListener('mousedown', handler);
  }, []);

  const handleSelect = async (code: SupportedLanguage) => {
    setOpen(false);
    await setLanguage(code);
  };

  return (
    <div ref={ref} className="relative">
      <button
        type="button"
        onClick={() => setOpen((o) => !o)}
        aria-expanded={open}
        aria-haspopup="listbox"
        className="inline-flex h-8 items-center gap-1.5 rounded-(--radius-md) border border-(--color-border) bg-(--color-surface) px-3 text-[13px] text-(--color-text) transition-colors hover:border-(--color-border-hover)"
      >
        {currentLang.nativeLabel}
        <span aria-hidden className="text-[10px] text-(--color-text-subtle)">▾</span>
      </button>

      {open && (
        <ul
          role="listbox"
          aria-label="언어 선택"
          className="absolute right-0 top-full z-50 mt-1 min-w-[120px] rounded-(--radius-md) border border-(--color-border) bg-(--color-surface) shadow-(--shadow-md)"
        >
          {languages.map((lang) => (
            <li key={lang.code} role="option" aria-selected={lang.code === current}>
              <button
                type="button"
                onClick={() => void handleSelect(lang.code)}
                className={`flex w-full items-center px-3 py-2 text-left text-[13px] transition-colors hover:bg-(--color-surface-alt) ${
                  lang.code === current ? 'font-semibold text-(--color-accent)' : 'text-(--color-text)'
                }`}
              >
                {lang.nativeLabel}
              </button>
            </li>
          ))}
        </ul>
      )}
    </div>
  );
}
```

- [ ] **Step 5-2: CommunityShell.tsx — placeholder → LanguageToggle 교체** (이슈 18)

```tsx
import { LanguageToggle } from '../../../components/LanguageToggle.js';
```

기존 disabled ActionButton 제거 후 교체:
```tsx
// 제거:
<ActionButton variant="neutralOutline" size="small" disabled title="언어 변경 (준비 중)">
  한국어
</ActionButton>

// 삽입:
<LanguageToggle />
```

- [ ] **Step 5-3: typecheck + 빌드**

```bash
cd apps/web && pnpm typecheck && pnpm build
```

- [ ] **Step 5-4: 커밋**

```bash
git add apps/web/src/components/LanguageToggle.tsx apps/web/src/pages/CommunityPage/parts/CommunityShell.tsx
git commit -m "feat(web): GG-COMM-013 언어 토글 실연결 — LanguageToggle 드롭다운, 전 서비스 언어 전환"
```

---

## Task 6: 게시글 "번역하기" UI

**Files:**
- Create: `apps/web/src/lib/api/translate.ts`
- Modify: `apps/web/src/pages/PostDetailPage/index.tsx`

- [ ] **Step 6-1: api/translate.ts 생성**

Create `apps/web/src/lib/api/translate.ts`:

```typescript
import type { SupportedLanguage } from '../i18n.js';

const BFF_URL = import.meta.env.VITE_BFF_URL ?? 'http://localhost:3000';

export interface PostTranslationResponse {
  postId: string;
  originalBody: string;
  translatedBody: string;
  targetLanguage: string;
  cached: boolean;
}

export async function translatePostContent(
  postId: string,
  targetLanguage: SupportedLanguage,
): Promise<PostTranslationResponse> {
  const res = await fetch(
    `${BFF_URL}/community/posts/${encodeURIComponent(postId)}/translate`,
    {
      method: 'POST',
      credentials: 'include',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ targetLanguage }),
    },
  );
  if (res.status === 404) throw new Error('POST_NOT_FOUND');
  if (res.status === 400) throw new Error('INVALID_LANG');
  if (!res.ok) throw new Error(`translate ${res.status}`);
  return (await res.json()) as PostTranslationResponse;
}
```

- [ ] **Step 6-2: PostDetailPage에 번역하기 버튼 + 결과 모달 추가** (이슈 7 — translateAnother t() 적용)

`apps/web/src/pages/PostDetailPage/index.tsx`에 import 추가:
```tsx
import { translatePostContent } from '../../lib/api/translate.js';
import type { SupportedLanguage } from '../../lib/i18n.js';
import { SUPPORTED_LANGUAGES } from '../../lib/i18n.js';
import { useLanguage } from '../../lib/useLanguage.js';
```

state/handler 추가 (기존 state 선언 다음):
```tsx
const { current: currentLang } = useLanguage();
const [translateOpen, setTranslateOpen] = useState(false);
const [translateLang, setTranslateLang] = useState<SupportedLanguage | ''>('');
const [translateLoading, setTranslateLoading] = useState(false);
const [translatedBody, setTranslatedBody] = useState<string | null>(null);
const [translateError, setTranslateError] = useState<string | null>(null);

const onTranslate = async (lang: SupportedLanguage) => {
  if (!detail) return;
  setTranslateLang(lang);
  setTranslateLoading(true);
  setTranslateError(null);
  try {
    const result = await translatePostContent(detail.postId, lang);
    setTranslatedBody(result.translatedBody);
  } catch {
    setTranslateError(t('post.translateError'));
  } finally {
    setTranslateLoading(false);
  }
};
```

좋아요/수정/삭제 ActionButton 다음에:
```tsx
<ActionButton
  variant="neutralOutline"
  size="small"
  onClick={() => { setTranslateOpen(true); setTranslatedBody(null); setTranslateError(null); setTranslateLang(''); }}
>
  {t('common:button.translate')}
</ActionButton>
```

article 닫기 전 모달 JSX — `"다른 언어로 번역"` 하드코딩 → `{t('post.translateAnother')}` (이슈 7):
```tsx
{translateOpen && (
  <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4">
    <div className="w-full max-w-lg rounded-(--radius-lg) bg-(--color-surface) p-5 shadow-(--shadow-lg)">
      <div className="mb-4 flex items-center justify-between">
        <h2 className="text-[17px] font-semibold">{t('post.translateTitle')}</h2>
        <button type="button" onClick={() => setTranslateOpen(false)}
          className="text-[13px] text-(--color-text-muted) hover:text-(--color-text)">
          {t('post.translateClose')}
        </button>
      </div>

      {!translatedBody && !translateLoading && (
        <div className="space-y-2">
          <p className="text-[13px] text-(--color-text-muted)">{t('post.translateSelectLang')}</p>
          <div className="flex flex-wrap gap-2">
            {SUPPORTED_LANGUAGES.filter((l) => l.code !== currentLang).map((l) => (
              <button key={l.code} type="button" onClick={() => void onTranslate(l.code)}
                className="rounded-(--radius-md) border border-(--color-border) px-3 py-1.5 text-[13px] hover:border-(--color-border-hover)">
                {l.nativeLabel}
              </button>
            ))}
          </div>
        </div>
      )}

      {translateLoading && <p className="text-[14px] text-(--color-text-muted)">{t('post.translateLoading')}</p>}
      {translateError && <p className="text-[14px] text-(--color-error)">{translateError}</p>}

      {translatedBody && !translateLoading && (
        <div className="space-y-4">
          <div>
            <p className="mb-1 text-[11px] font-semibold uppercase tracking-wider text-(--color-text-subtle)">
              {t('post.translateResult')} ({SUPPORTED_LANGUAGES.find(l => l.code === translateLang)?.nativeLabel})
            </p>
            <p className="whitespace-pre-wrap text-[14px] leading-relaxed">{translatedBody}</p>
          </div>
          <div className="border-t border-(--color-border) pt-3">
            <p className="mb-1 text-[11px] font-semibold uppercase tracking-wider text-(--color-text-subtle)">
              {t('post.translateOriginal')}
            </p>
            <p className="whitespace-pre-wrap text-[14px] leading-relaxed text-(--color-text-muted)">{detail?.body}</p>
          </div>
          <button type="button"
            onClick={() => { setTranslatedBody(null); setTranslateLang(''); setTranslateError(null); }}
            className="text-[12px] text-(--color-text-muted) hover:text-(--color-text)">
            {t('post.translateAnother')}
          </button>
        </div>
      )}
    </div>
  </div>
)}
```

- [ ] **Step 6-3: typecheck + 빌드**

```bash
cd apps/web && pnpm typecheck && pnpm build
```

- [ ] **Step 6-4: 커밋**

```bash
git add apps/web/src/lib/api/translate.ts apps/web/src/pages/PostDetailPage/index.tsx
git commit -m "feat(web): 게시글 '번역하기' UI — 언어 선택, 번역 결과 모달, translateAnother t() 적용"
```

---

## Task 7: 전체 Green 검증

- [ ] **Step 7-1: BFF typecheck**

```bash
cd apps/bff && pnpm typecheck
```

- [ ] **Step 7-2: Web typecheck + 빌드**

```bash
cd apps/web && pnpm typecheck && pnpm build
```

- [ ] **Step 7-3: LLM 서비스 health 확인**

```bash
curl -s http://localhost:8000/health | python3 -m json.tool
```

Expected: `"ok": true`. `/translate-post` 및 `/translate-bundle` 라우트 등록 확인.

- [ ] **Step 7-4: deep-diff 최종 확인 — 미번역 키 0** (이슈 8/14)

Step 3-5의 deep-diff 스크립트 재실행.

Expected: `deep-diff OK: 0 missing keys across all 40 bundles.`

- [ ] **Step 7-5: 언어 전환 동작 수동 검증**

1. `cd apps/web && pnpm dev`
2. 브라우저 `/community` 접속
3. 언어 토글 드롭다운 → "English" 선택
4. 커뮤니티 제목, 버튼, Sidebar, Header 전부 영어로 변경 확인
5. 새로고침 후 영어 유지 (localStorage 영속) 확인
6. "한국어" 재선택 → 복구 확인
7. `/admin` → 관리자 패널 전체(탭, 상세 패널, reason textarea 안내) 언어 전환 확인 (이슈 1)
8. 모바일 뷰포트(375px) → MobileShell 탭 라벨(목록/필터/채팅) 언어 전환 확인 (이슈 2)
9. FilterSearchPanel → 필터 5종 옵션 라벨 언어 전환 확인 (이슈 3)

- [ ] **Step 7-6: 게시글 번역 수동 검증**

1. 게시글 상세 접속 → "번역하기" 버튼 클릭
2. 언어 선택 → 번역 결과 모달 표시 확인
3. "다른 언어로 번역" 버튼 동작 확인 (t() 적용 확인)
4. 2회차 요청 → BFF 로그 `cached: true` 확인

- [ ] **Step 7-7: 최종 커밋**

```bash
git add -A
git commit -m "feat(web,bff,llm): Slice 7 완료 — i18n 6개국어 전화면 + 게시글 번역 + GG-COMM-013 언어 토글"
```

---

## 리뷰 이슈 해소 매핑

| 이슈 | 내용 | 해소 위치 |
|---|---|---|
| 1/9 | admin components/* 패널 modify 목록·키셋 누락 | Task 2C-1 admin.json detail-level 키 + 2C-3 패널 전부 명시 |
| 2/10 | MobileShell, BottomSheet 누락 | Task 2A-5/6 + navigation.json mobile.* + common.json aria.* |
| 3/11/12 | 필터 5종 옵션·enum·업로더 폼 누락 | navigation.json filter.*/subscription.* + uploader.json category/companion + Task 2A-9, 2B-5 |
| 4/13/21 | 다국어 폰트 fallback 미반영 | Task 0 DESIGN.md 선행 개정 + Task 1-5 index.css |
| 5 | Suspense 부재 + 서술-구현 불일치 | i18n.ts `react: {useSuspense:false}` + Architecture 문구 정정 |
| 6 | redis-client.ts 불필요 수정 항목 | 파일 지도에서 '수정 없음' 명시 |
| 7 | "다른 언어로 번역" 하드코딩 | community.json post.translateAnother + Task 6-2 t() 적용 |
| 8/14 | 미번역 키 0 검증 부족 | generate 스크립트 실패 시 exit 1 + Step 3-5/7-4 deep-diff |
| 15 | 와일드카드 키 정의 부재 | 파일별 수정 대상 개별 명시 (ComposeModal/CommentTree/ReviewsSection 등) |
| 16 | react-i18next 미설치 | Task 1-1 pnpm add 4개 패키지 |
| 17 | I18nextProvider 미적용 | Task 1-4 main.tsx 명시 |
| 18 | CommunityShell placeholder 미교체 | Task 5-2 |
| 19 | BFF translate 라우트 미등록 | Task 4-3 app.ts |
| 20 | translate.py 미생성 | Task 3-1/3-2 |
| 22 | Redis 키 충돌/분리 미정 | translation-cache.ts `post:translation:` prefix 명시 |
| 23 | LLM 에러 처리 미정 | routes/translate.ts graceful degradation 정책 명시 |
